{
  "name": "@findify/grault",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "config": {
    "access": "public"
  },
  "scripts": {
    "postinstall": "bower install --production",
    "clean": "del lib output",
    "build": "pulp browserify --standalone --skip-entry-point -m Grault -O -t lib/index.js",
    "build:test": "npm run build",
    "build:test:watch": "pulp --watch browserify --standalone --skip-entry-point -m Grault -O -t lib/index.js",
    "test": "pulp test"
  },
  "devDependencies": {
    "browserify": "^14.4.0",
    "del-cli": "^1.1.0",
    "npm-check-updates": "^2.12.1",
    "pulp": "^12.0.1"
  },
  "author": "Findify <yourfriends@findify.io>",
  "license": "WTFPL"
}
