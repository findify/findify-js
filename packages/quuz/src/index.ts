import eight from '@findify/qux';
import { qux } from '@findify/qux';
import { quux } from '@findify/quux';

export const quuz1 = x => qux(x) * 2 + quux(x + 1) + eight() - 2;
export const quuz2 = () => 'quuz2';
